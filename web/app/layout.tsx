import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Vaulty Agent — AI Form Filler & Copilot for Chrome",
  description:
    "Vaulty Agent auto-fills web forms with your saved profile, summarizes pages, drafts emails, and assists you on any website — right inside Chrome.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
